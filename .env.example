# Obrigatório. Nunca exponha esta chave no browser — só a rota de servidor a lê.
ANTHROPIC_API_KEY=

# Opcional no MVP. Sem estas variáveis o app roda inteiro em memória:
# a história funciona, mas nada é persistido ao recarregar a página.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
